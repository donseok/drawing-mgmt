# 백로그 — R26 이후 작업

| 항목 | 값 |
|---|---|
| 작성일 | 2026-04-26 |
| 기준 main HEAD | `4f58377` (R25 통합 후) |
| 선행 산출물 | `docs/잔여작업.md` (R3c~R25 처리 이력) |
| 의도 | R25까지 앱 기능 누적 완료. 운영 시스템화 + 잔여 도메인 패리티를 카드로 분해. |

R25까지 누적 결과(2026-04-26):

- **앱 기능**: As-Is 패리티 약 70~80% (자료 CRUD/검색/결재/로비/트랜스미털/첨부/즐겨찾기/폴더 작업/일괄 등록·이동·복사·다운로드/뷰어 4 phase + Worker)
- **인프라/운영**: 거의 미진 (dev compose만 존재)
- **테스트/QA**: 미진 (수동 typecheck + build만)
- **사용자 메모 단계**: 1(자체 뷰어) Phase 1~4 + 성능, 2(폴더 메뉴 + 커스터마이즈) — 모두 1차 완료. 3(사용자 지시 추가) = 이 백로그 전부.

---

## 0. 우선순위 한 눈에

| 우선순위 | 영역 | 카드 ID 묶음 | 추정 라운드 |
|---|---|---|---|
| **P0 (운영 가능 도달)** | 마이그레이션 + 변환 워커 + Prisma Migrate + 인증 SSO + ESLint | M-*, V-*, D-*, A-*, X-LINT | 6~8 |
| **P1 (As-Is 패리티 마무리)** | 관리자 콘솔 풀 UI + 프린트/PDF + 환경설정 + 자료유형 admin UI | U-*, P-*, S-*, R26 | 4~6 |
| **P2 (품질·보안)** | 테스트 인프라 + WCAG audit + 보안 추가 (CSP/CSRF/Rate) + 감사 강화 | T-*, AC-*, SEC-* | 5~7 |
| **P3 (관측·운영자 도구)** | 모니터링 + 로그 수집 + impersonation + 통계 대시보드 + slow query | O-* | 3~4 |
| **P4 (확장·미래)** | RAG 챗봇 풀 + Notification 테이블 + 다국어 + PWA + 마크업 정밀 | E-* | 5~8 |

---

## 1. 자체 DWG 뷰어 — 후속 (V)

| ID | 우선순위 | 카드 |
|---|---|---|
| V-1 | P1 | HATCH 패턴 정확한 polygon 클립 (현재 sample-based, 미세 누설 가능) |
| V-2 | P2 | 라인 두께 (Line2 promote — 현재 1px 고정) |
| V-3 | P2 | SDF 폰트 / 텍스처 atlas (TEXT 캐싱) — 큰 도면 텍스트 다수일 때 VRAM |
| V-4 | P3 | LOD (줌 레벨별 segment 수 조절) |
| V-5 | P2 | 측정 도구 정밀도 검증 (다점 거리/면적/각도) — 기존 MeasurementOverlay 회귀 |
| V-6 | P2 | 마크업 저장 + 공유 (현재 MeasurementOverlay만, 영구 저장 없음) |
| V-7 | P3 | 도면 비교 diff viewer (As-Is "리비전 비교" 핵심) |

---

## 2. As-Is TeamPlus 패리티 마무리 (P)

| ID | 우선순위 | 카드 |
|---|---|---|
| P-1 | P1 | **프린트** (PDF 변환, monochrome.ctb / A3칼라.ctb 대응) — Ghostscript subprocess |
| P-2 | P1 | **PDF 병합** (단일/일괄) |
| P-3 | P1 | Rev 이력 등록 (중간 빠진 이력 채우기 — admin 한정) |
| P-4 | P1 | 자료 이동 시 도면번호 변경 옵션 |
| P-5 | P1 | 사용자 환경설정 — 비밀번호 변경, 서명 파일 업로드 |
| P-6 | P3 | 창 배열 (계단/바둑판/전체 — 다중 도면 비교 뷰) |

---

## 3. 관리자 콘솔 풀 UI (U) — BE는 일부 있고 FE 미구현

| ID | 우선순위 | 카드 |
|---|---|---|
| U-1 | P1 | **자료유형/속성 관리 UI** (R25 BE 짝맞춤) — 폼 + 속성 빌더 |
| U-2 | P1 | 사용자 관리 풀 UI (현재 endpoint만, FE 단순) — 잠금/해제, 비밀번호 초기화 |
| U-3 | P2 | 조직 관리 (드래그 트리) |
| U-4 | P2 | 그룹 관리 (사용자 ↔ 그룹 매트릭스) |
| U-5 | P1 | **폴더 권한 매트릭스 UI** — 폴더 × 사용자/그룹/조직 × 8개 비트(view/edit/delete/approve/download/print) |
| U-6 | P1 | **자동발번 규칙 빌더** (FOLDER_CODE + LITERAL + SEQUENCE + YEAR 조합 UI) |
| U-7 | P2 | 통계/리포트 화면 (실시간 접속, 등록 현황, 등급별 자료) |
| U-8 | P2 | 공지사항 작성/관리 폼 (메인/팝업 토글) |

---

## 4. 검색 강화 (S)

| ID | 우선순위 | 카드 |
|---|---|---|
| S-1 | P2 | PDF 본문 검색 (pgvector + RAG 또는 trgm) |
| S-2 | P2 | 이력 검색 (시간 범위 + 사용자 + action) |
| S-3 | P3 | 사용자별 저장된 보기 (현재 4개 하드코딩 — UserSavedView 모델 신설) |
| S-4 | P3 | 검색 자동완성 (도면번호/자료명 fuzzy) |
| S-5 | P3 | 최근 본 자료 timeline |

---

## 5. AI 챗봇 풀 구현 (E)

| ID | 우선순위 | 카드 |
|---|---|---|
| E-1 | P4 | RAG 파이프라인 (ManualChunk + pgvector — 현재 stub canned response) |
| E-2 | P4 | Rule mode 라우팅 (CHAT_MODE=auto 자동 선택) |
| E-3 | P4 | 도구 호출 (도면 검색 / 결재함 조회 / 통계) |
| E-4 | P4 | 비용/토큰 모니터링 (현재 schema에 ChatMessage.tokensIn/Out 컬럼 있음 — 대시보드 부재) |

---

## 6. 알림 / 결재 강화 (N)

| ID | 우선순위 | 카드 |
|---|---|---|
| N-1 | P2 | **진짜 Notification 테이블** (현재 ActivityLog 24h 윈도 합성) — 마킹/필터/숨김 |
| N-2 | P3 | 이메일/카카오/SMS push 통합 |
| N-3 | P2 | 결재선 템플릿 (자주 쓰는 결재선 저장/재사용) |
| N-4 | P3 | 결재 미루기 / 회수 UI 보강 (BE는 R4a 완료) |

---

## 7. 인증 / SSO (A)

| ID | 우선순위 | 카드 |
|---|---|---|
| A-1 | P0 | **Keycloak 연동** (사내 AD/LDAP) — 현재 Credentials provider만 |
| A-2 | P1 | SAML / OIDC SSO |
| A-3 | P2 | MFA (TOTP) |
| A-4 | P2 | 비밀번호 정책 (만료·복잡도) — 현재 lockout만 |
| A-5 | P3 | 세션 관리 콘솔 (관리자가 강제 로그아웃) |
| A-6 | P3 | 사용자 impersonation (관리자 디버깅용) |

---

## 8. DB 운영 (D)

| ID | 우선순위 | 카드 |
|---|---|---|
| D-1 | P0 | **Prisma Migrate 정식 도입** (현재 manual SQL only — 재현/롤백 어려움) |
| D-2 | P1 | 인덱스 튜닝 + EXPLAIN 분석 (대용량 검색) |
| D-3 | P2 | pgvector 차원/모델 결정 + benchmark (현재 1536/OpenAI 가정) |
| D-4 | P2 | 데이터 보존 정책 (soft-delete → hard-delete 주기 — 산업안전보건법 검토) |
| D-5 | P0 | **백업 자동화** (pg_dump cron) + DR 시나리오 |

---

## 9. 파일 스토리지 / 변환 파이프라인 (V) — 도메인 핵심 인프라

| ID | 우선순위 | 카드 |
|---|---|---|
| V-INF-1 | P1 | 로컬 디스크 → **MinIO/S3 전환** (대용량 운영) |
| V-INF-2 | P2 | **청크 업로드** (50MB+ DWG — 현재 단일 multipart) |
| V-INF-3 | P2 | 바이러스 스캔 (ClamAV subprocess) |
| V-INF-4 | P0 | **변환 워커 풀 자동화** (BullMQ + LibreDWG/ODA — 현재 dev ingest만 동기) |
| V-INF-5 | P1 | 변환 큐 모니터링 / 재시도 정책 |
| V-INF-6 | P1 | DXF/PDF preview + 썸네일 자동 생성 (업로드 시 자동 트리거) |
| V-INF-7 | P3 | 체크섬 검증 자동화 (정기 무결성 체크) |

---

## 10. 데이터 마이그레이션 (M) — 가장 큰 운영 위험

| ID | 우선순위 | 카드 |
|---|---|---|
| M-1 | P0 | **As-Is TeamPlus → 신 시스템 데이터 이관 스크립트** (자료/사용자/폴더/권한) |
| M-2 | P0 | 매핑 테이블 정의 (As-Is class/folder code → 신 시스템) |
| M-3 | P1 | 운영 중 dual-write 또는 단계별 cutover 전략 |
| M-4 | P1 | 이관 검증 (sample diff + count 비교) |
| M-5 | P2 | 롤백 계획 (이관 실패 시 복귀) |

---

## 11. 인프라 / 배포 (X)

| ID | 우선순위 | 카드 |
|---|---|---|
| X-1 | P0 | **Dockerfile + 운영 docker-compose** (현재 dev compose만) |
| X-2 | P0 | 환경별 설정 (dev/staging/prod) + secrets 관리 (Vault / Doppler / sealed secrets) |
| X-3 | P0 | **CI/CD 파이프라인** (GitHub Actions: lint/typecheck/build/test/migrations/push) |
| X-4 | P1 | 모니터링 (Prometheus + Grafana 또는 단순 pino → Loki) |
| X-5 | P1 | 로그 수집/조회 (구조화 로그 + 필터) |
| X-6 | P1 | HTTPS / 사내 CA 인증서 |
| X-7 | P2 | APM (지연/에러 트래킹) |
| X-LINT | P0 | **ESLint 정식 config** (현재 미설정 — `pnpm lint` 실패) |
| X-FMT | P1 | Prettier + pre-commit hook |
| X-SB | P3 | Storybook (UI 컴포넌트 카탈로그) |

---

## 12. 보안 (SEC)

| ID | 우선순위 | 카드 |
|---|---|---|
| SEC-1 | P1 | CSRF 토큰 강화 (Auth.js 기본 외) |
| SEC-2 | P1 | Content Security Policy 설정 |
| SEC-3 | P1 | API 전반 Rate limiting (현재 chat만) |
| SEC-4 | P2 | 의존성 취약점 스캔 (npm audit / Snyk CI 통합) |
| SEC-5 | P2 | 침투 테스트 (외부 / 내부) |
| SEC-6 | P2 | 감사 로그 변조 방지 (해시 체인 / append-only WORM) |
| SEC-7 | P2 | 보안등급 정의 명확화 (As-Is 매뉴얼 3·4등급 동일 — 명세 오류) |

---

## 13. 접근성 / 국제화 / 모바일 (AC)

| ID | 우선순위 | 카드 |
|---|---|---|
| AC-1 | P2 | WCAG 2.1 AA 풀 audit (현재 R25에서 일부 focus-visible) |
| AC-2 | P3 | 다국어 (i18n — 현재 한국어 hard-coded) |
| AC-3 | P3 | 스크린 리더 검증 (NVDA / VoiceOver) |
| AC-4 | P2 | 태블릿/모바일 UX 풀 테스트 (R25에서 일부 정리) |
| AC-5 | P3 | 터치 제스처 (뷰어 핀치 줌, 마크업) |
| AC-6 | P4 | PWA / 오프라인 모드 |

---

## 14. 테스트 인프라 (T) — 전혀 미진

| ID | 우선순위 | 카드 |
|---|---|---|
| T-1 | P0 | **단위 테스트 framework** (vitest 도입 + sample 5~10개) |
| T-2 | P1 | 통합 테스트 (Prisma test DB + supertest) |
| T-3 | P0 | **E2E 테스트** (Playwright — MCP 가용. 핵심 흐름 5개부터: 로그인/검색/뷰어/결재/트랜스미털) |
| T-4 | P3 | 시각 회귀 (Playwright screenshot diff) |
| T-5 | P2 | 부하 테스트 (k6) — 1000 동접 시나리오 |
| T-6 | P2 | 커버리지 보고 + threshold gate |

---

## 15. 운영자 도구 / 관측 (O)

| ID | 우선순위 | 카드 |
|---|---|---|
| O-1 | P3 | 통계 대시보드 (DAU/MAU, 변환 지연, 결재 대기) |
| O-2 | P3 | 데이터 수정 콘솔 (긴급 패치용 — admin 전용) |
| O-3 | P3 | Slow query 분석 (pg_stat_statements 노출) |
| O-4 | P3 | API Key 발급/취소 UI (스키마는 ApiKey 모델 있음, 현재 admin/integrations placeholder) |
| O-5 | P3 | 외부 integration 가이드 (페이로드 예시 + 인증) |

---

## 16. 문서 / 법적 (DOC)

| ID | 우선순위 | 카드 |
|---|---|---|
| DOC-1 | P1 | **OpenAPI 자동 문서** (라우트별 zod schema → openapi spec — `next-openapi-gen` 등) |
| DOC-2 | P1 | 사용자 매뉴얼 (As-Is User_Manual.pdf → 신 시스템 buttons/screenshots) |
| DOC-3 | P1 | 운영자 매뉴얼 (As-Is Manager_Manual.pdf 대응) |
| DOC-4 | P2 | 개발자 가이드 (현 CLAUDE.md 정식화 + onboarding) |
| DOC-5 | P1 | 개인정보 처리 방침 / 사용 약관 |
| DOC-6 | P1 | 데이터 보존 정책 (산업안전보건법 도면 보존 검토) |
| DOC-7 | P2 | 변경 이력 / 릴리스 노트 자동화 (changesets 등) |

---

## 17. QA / 사용자 테스트 (Q)

| ID | 우선순위 | 카드 |
|---|---|---|
| Q-1 | P2 | 2차 사용자 테스트 라운드 (1차 = `사용자테스트버그.md`) |
| Q-2 | P3 | 권한 분기 매트릭스 테스트 (협력사/일반/관리자) |
| Q-3 | P3 | 큰 도면 fixture (50k+ 엔티티) viewer 성능 회귀 측정 |
| Q-4 | P3 | 에러 ID 기반 백엔드 로그 트레이싱 |

---

## 18. 마이크로 도메인 기능 (small wins)

| ID | 우선순위 | 카드 |
|---|---|---|
| 도메인-1 | P3 | 자료 즐겨찾기 (현재 핀과 별개로 quick favorites — 짧은 별칭) |
| 도메인-2 | P3 | 활동 피드 home page 강화 (현재 정적 메트릭) |
| 도메인-3 | P3 | 도면 viewer toggle (PDF vs DWG vs Thumbnail 빠른 전환) |
| 도메인-4 | P4 | 자료 만료 알림 (Lobby + 자료 두 종류) |
| 도메인-5 | P4 | 자료 위임 (휴가 시 결재선 자동 대체) |

---

## 19. 권장 시작 순서 (내일 첫 라운드 후보)

다음 라운드는 **단일 트랙**(앱 기능) vs **운영 트랙**(인프라) 두 갈래 — 사용자 결정 필요.

### 옵션 A — As-Is 패리티 마무리 트랙 (사용자 가시 빠름)
1. **R26**: U-1(자료유형/속성 admin UI) + P-5(사용자 환경설정) — R25 BE 짝맞춤 + 사용자별 고통점 해소
2. **R27**: U-5(폴더 권한 매트릭스 UI) + U-6(자동발번 규칙 빌더) — 관리자 콘솔 마무리
3. **R28**: P-1(프린트) + P-2(PDF 병합) — 도메인 핵심 미진

### 옵션 B — 운영 가능 시스템 트랙 (배포 가능 도달)
1. **R26**: D-1(Prisma Migrate) + X-1(Dockerfile/운영 compose) + X-LINT(ESLint 정식)
2. **R27**: X-3(CI/CD) + T-1(vitest) + T-3(Playwright 5 핵심 흐름)
3. **R28**: A-1(Keycloak) + V-INF-4(변환 워커 자동화) + M-1(데이터 이관 script 골격)

### 옵션 C — 혼합 (현실적, 추천)
1. ~~**R26**: U-1(자료유형 UI) + X-LINT(ESLint) + T-3(Playwright 1~2 흐름)~~ **완료 2026-04-26**
2. ~~**R27**: P-5(환경설정) + D-1(Prisma Migrate) + DOC-1(OpenAPI)~~ **완료 2026-04-26**
3. ~~**R28**: U-5(권한 매트릭스 UI) + V-INF-4(변환 워커) + SEC-1/2/3(보안 quick wins)~~ **완료 2026-04-27**
4. ~~**R29**: U-2(사용자 관리 풀 UI) + N-1(진짜 Notification 테이블) + V-INF-6(자동 썸네일 생성)~~ **완료 2026-04-27** (외부 의존 없는 패리티/품질 트랙)
5. ~~**R30**: U-3(조직 트리) + U-4(그룹 admin) + V-1(HATCH 정확 클립)~~ **완료 2026-04-27** (트랙 A 패리티+뷰어 후속)
6. ~~**R31**: P-1(프린트/PDF) + V-INF-2(청크 업로드)~~ **완료 2026-04-27** (트랙 B 도메인 핵심)
7. ~~**R32**: X-1(Dockerfile) + X-3(CI/CD) + T-1(vitest 인프라) + DOC outline~~ **완료 2026-04-27** (트랙 C 운영 가능 시스템)
8. ~~**R33**: D-5(백업 자동화 — pg_dump cron + admin) + A-1(Keycloak/OIDC SSO)~~ **완료 2026-04-27**
9. ~~**R34**: V-INF-1(MinIO/S3 storage 전환 — 추상화 + 5 라우트 + 워커 통합)~~ **완료 2026-04-27**
10. ~~**R35**: N-1(메일 채널 — nodemailer SMTP + 마이그레이션 0009 + me/preferences + settings 토글) + DOC-2/DOC-4 1차 본문~~ **완료 2026-04-27** (마지막 자동 라운드)
11. ~~**R36**: V-INF-3(ClamAV scan + 6 status guards + 마이그 0010) + T-2(통합 테스트 infra + 5 샘플)~~ **완료 2026-04-27**
12. ~~**R37**: A-2(SAML SSO + HMAC bridge token) + AC-1(WCAG audit + P0/P1 일부 fix) + V-2(Line2 + lineWeight)~~ **완료 2026-04-27**
13. ~~**R38**: N-2(SMS + 카카오 알림톡 — twilio Apache 2.0 lazy + NCP SENS native fetch + 마이그 0012 + sms/kakao 큐/워커 + settings 확장)~~ **완료 2026-04-27**

**잔여 P1 5개 카드(V-INF-3 + T-2 + A-2 + AC-1 + N-2 + V-2) R36~R38 자동 분할 완료.** 추가 카드는 사용자 결정.

---

## 20. 변경 이력

| 날짜 | 변경 |
|---|---|
| 2026-04-26 | 초기 작성 (R25 통합 직후 — 운영/인프라/QA 갭 명세) |
| 2026-04-26 | R26 완료: U-1, X-LINT, T-3. R27 완료: P-5, D-1, DOC-1. 다음=R28 |
| 2026-04-27 | R28 완료: U-5(폴더 권한 매트릭스 UI), V-INF-4(변환 워커 자동화 + LibreDWG fallback), SEC-1/2/3(CSRF/CSP/Rate limit). 5인 팀 worktree 격리 병렬 실행 검증. 다음=R29 |
| 2026-04-27 | R29 완료: U-2(사용자 관리 풀 UI 5 다이얼로그), N-1(Notification 테이블 + 마이그레이션 0005 + 알림 4지점 wire), V-INF-6(자동 썸네일 sharp + ConversionJob 컬럼 + 마이그레이션 0006). schema.prisma 동시 수정 자동 머지 성공. 다음=R30 |
| 2026-04-27 | R30 완료: U-3(조직 트리 admin + 5 endpoints), U-4(그룹 멤버십 매트릭스 + 5 endpoints), V-1(HATCH analytic line-vs-polygon clip — 외부 의존 0). agent base SHA echo 가드 정착. 다음=R31 |
| 2026-04-27 | R31 완료: P-1(PrintDialog + DXF→PDF pdf-lib 워커 + 2 endpoints), V-INF-2(Upload 모델/마이그레이션 0007 + 4 endpoints + chunk-upload helper). backend agent sandbox 사고로 main 트리 leak → PM Phase 4 정리 후 재머지로 복구. 다음=R32 |
| 2026-04-27 | R32 완료: X-1(web/worker Dockerfile multistage + LibreDWG source build + non-root), X-1.c(docker-compose.prod.yml — pg/redis/web/worker), X-3(GitHub Actions CI lint+typecheck+build), T-1(vitest + happy-dom + 25 assertion 3 file), DOC outline(사용자 매뉴얼 9챕터 + 개발자 가이드 8항목). 머지 충돌 0건, sandbox 사고 0건. 다음=R33 |
| 2026-04-27 | R33 완료: D-5(Backup 모델 + 마이그레이션 0008 + pg_dump/tar 워커 + admin/backups 3 endpoint + cron repeatable), A-1(Keycloak provider 조건부 + signIn auto-provisioning + login SSO 버튼). designer 격리 위반 1회(main 직접 ff됨)로 운영 학습. 다음=R34 |
| 2026-04-27 | R34 완료: V-INF-1(packages/shared/storage interface + LocalStorage/S3Storage + @aws-sdk/client-s3 + 핵심 5 라우트 마이그레이션 + 워커 storage 통합 + /admin/storage 페이지 + 11 vitest cases). pnpm-lock 충돌(backend+viewer 둘 다 SDK 추가) + storage-local.test.ts 충돌(backend+frontend 둘 다 작성) — PM 봉합. 다음=R35 |
| 2026-04-27 | R35 완료: N-1(User.notifyByEmail + 마이그레이션 0009 + lib/mail.ts nodemailer + mail-queue + enqueueNotification fan-out + 메일 워커 + /me/preferences + settings Switch 토글) + DOC-2(USER_MANUAL.md 1060 LOC) + DOC-4(DEVELOPER_GUIDE.md 850 LOC). pnpm-lock 충돌 봉합(R34 패턴 재사용). 사용자 "순차적으로 전부" 요청 8개 카드(D-5/A-1/V-INF-1/N-1/DOC-2/DOC-4) R33~R35 자동 분할 완료. |
| 2026-04-27 | R36 완료: V-INF-3(Attachment.virusScanStatus + 마이그 0010 + ClamAV worker (subprocess GPL 격리) + 5 라우트 INFECTED 가드 + /admin/scans + scan-queue + 알림 발생), T-2(integration test infra: setup/helpers/seed + 5 샘플 .int.test.ts + vitest VITEST_INTEGRATION 분기). backend가 main 트리 작업 사고 1회 자체 회복. shared/conversion.ts 충돌 봉합(backend 우선 ours). 다음=R37 |
| 2026-04-27 | R37 완료: A-2(SAML SSO — node-saml + HMAC bridge token + Auth.js v5 v5 통합 + 마이그 0011 + 12 vitest), AC-1(WCAG audit 13 화면 + P0/P1 frontend focus-visible/aria 수정), V-2(Line2 + lineWeight DXF 그룹 370 + 10 vitest, 총 58 pass). .env.example 충돌 봉합. 다음=R38 |
| 2026-04-27 | R38 완료: N-2(User.phoneNumber/notifyBySms/notifyByKakao + 마이그 0012 + lib/sms.ts twilio Apache 2.0 lazy + lib/kakao.ts native fetch + sms/kakao queue/worker + settings 채널 확장 + 전화번호 입력). 라이선스 GPL/AGPL 의존 0건. backend main 누출 자체 회복. R36~R38 잔여 P1 모두 완료. |
| 2026-04-27 | R39 partial 완료: A-3 MFA(enroll/confirm/disable + lib/totp + 마이그 0013), A-4 password-policy lib + passwordPrev/passwordChangedAt 컬럼, V-5 측정 audit + 40 vitest cases(98 pass), MfaSection + PasswordSection. **미완 → R40 흡수**: auth.ts MFA bridge, /auth/mfa/verify, /admin/security/audit, /admin/users/[id]/expire-password, CI audit step, /login/mfa 페이지, /admin/security 페이지. agent rate limit으로 사고 후 PM이 worktree 부분 작업 commit + 통합. otpauth TOTPOptions namespace 누락 fix. |
| 2026-04-28 | R40 완료: R39 흡수(auth.ts 3-mode credentials + lib/mfa-bridge + /auth/mfa/verify + /admin/security/audit GET + /admin/users/[id]/expire-password + 마이그 0014 PDF FTS) + S-1 PDF 본문 검색(pdfjs-dist Apache 2.0 기반 pdf-extract 워커 + ConversionJob DONE 후 enqueue + objects 라우트 FTS OR-union + ts_headline pdfSnippet) + MFA 2단계 로그인(/login/mfa 단일 input + 6자 자동 submit + 복구 코드 토글 + 5회 잠금) + /admin/security 페이지(SecurityAuditCard 4 카드 + [지금 검사] mutation, POST /audit) + CI npm audit job(continue-on-error). PM이 R39 untracked 산출물을 R40 base commit `f432820`로 정리해 designer/FE/BE worktree base SHA를 통일. main 격리 가드 위반 0건, 머지 충돌 0건, typecheck/build 양쪽 pass. |
| 2026-04-28 | R41 완료: PdfExtractStatus enum + Attachment 컬럼 3종(pdfExtractStatus/At/Error) + 마이그 0015(idempotent + backfill — contentText가 있는 row를 DONE으로 회수) + pdf-extract 워커 status 트랜지션(EXTRACTING/DONE/FAILED) + 메인 워커 PDF 없는 케이스 SKIPPED 마킹 + /admin/pdf-extracts 페이지(카운트 5 카드 + 필터 + 재시도 ConfirmDialog + optimistic PENDING flip + 5s 폴링) + /admin/pdf-extracts/[id]/retry 라우트(상태 가드: FAILED/SKIPPED만) + lib/pdf-extract-queue.ts BullMQ Queue 헬퍼 + audit 라우트 advisories 배열 합성(parseAuditOutput → parseAuditDetail) + VulnerabilitiesTable 컴포넌트(severity 정렬 + 50건 더보기 + 외부 링크 보안 가드) + /admin/security 카운트 카드 클릭 필터(ring-2 + bg-bg-subtle + aria-pressed 단일 토큰) + /admin/pdf-extracts 동일 토큰 재사용 + admin-groups에 "PDF 본문 추출"(FileText 아이콘) 추가. designer Write 도구가 main 트리에 leak 1회 → mv로 자체 회복. 머지 충돌 0건, typecheck/build 양쪽 pass. |
| 2026-04-28 | R42 완료(폴리시 라운드, designer skip): 검색 ranking 통합(trgm score 컬럼 노출 + FTS ts_rank + FTS_WEIGHT=1.5 가중 + hitMap unified score + matchSource 'meta'/'pdf'/'both'/null 노출 + q&&!sortBy&&!cursor 첫 페이지에서만 ranking 정렬 강제 절충) + /admin/pdf-extracts ?status= URL sync + /admin/security ?severity= URL sync + ObjectTable에 MatchSourceChip(BE 미배포 시 forward-compat — `?? null`). backend agent의 sandbox snapshot 사고 1회: Edit 결과가 worktree에 안 묻고 main 트리로 leak 후 자체 Python 재적용으로 회복. PM Phase 4에서 leak 발견 → worktree commit과 byte-equal 확인 후 `git checkout --` 정리 + 정식 머지. typecheck/build 양쪽 pass. |
| 2026-04-28 | R43 완료(BE only 라운드, designer/FE skip): 검색 cursor+ranking 정확 페이지네이션(`useRankedPagination` 가드 분기 — q+!sortBy일 때 candidateIds idxMap의 position 기반 페이징, Prisma cursor 미사용. cursor.id 못 찾으면 첫 페이지로 fallback. over-fetch 버퍼 `min(limit*2, 100)` + 권한 필터 후 limit 잘라냄. selectShape `satisfies` 추출로 두 경로 select 일관 유지) + ts_headline multi-fragment(`MaxFragments=3 + MaxWords=15 + MinWords=4 + FragmentDelimiter= … `, U+2026 공백 둘러쌈, FE 변경 0건). backend agent가 R42 sandbox 사고 가드(`git status --short` 즉시 검증)를 prompt에 넣은 결과 누출 사고 0건. typecheck/build 양쪽 pass. R42에서 deferred한 "accurate cursor-rank pagination" 카드 마무리. |
| 2026-04-28 | R44 완료(FE only 라운드 — Playwright E2E): WBS 4.4.1 "E2E 시나리오 5건" 충족. 기존 login×2 + search×2(R26 T-3) + 신규 3종 = `apps/web/e2e/object-lifecycle.spec.ts`(자료 등록 → 상세 → 삭제), `apps/web/e2e/approval.spec.ts`(상신 → 셀프 승인 → 상태 전이), `apps/web/e2e/lobby-transmittal.spec.ts`(트랜스미털 → 로비 → 검토 회신). admin storageState 재사용, getByRole/getByLabel/id locator 우선. test.slow() + waitForResponse / waitForURL로 안정화. typecheck pass + `playwright test --list` 8 tests in 6 files 인식 확인(setup 1 + chromium 7). 실제 실행은 dev server + DB seed 의존이라 PM 검증 범위 밖 — 사용자 다음 dev 사이클에서 실 흐름 점검 권장. 머지 충돌 0건, 격리 위반 0건. |
| 2026-04-28 | R45 완료(docs only — PM 직접 작업, agent skip): USER_MANUAL.md 챕터 12 신설(R36~R44 사용자 관점 catch-up — SAML/MFA/비밀번호 정책/SMS·카카오 알림/PDF 본문 검색/검색 ranking/multi-fragment/cursor 페이지네이션/admin 의존성 보안/PDF 추출 모니터/바이러스 스캔 모니터/측정 정밀도/선 가중치/접근성). DEVELOPER_GUIDE.md 11장 신설(R36~R44 dev-facing — 통합 테스트 인프라/ClamAV 워커/SAML+HMAC bridge token/WCAG audit/Line2+lineWeight/SMS·카카오 큐 분리/MFA·비밀번호 lib/PDF FTS 마이그+워커+라우트/admin 페이지+CI audit/E2E 5건/라이선스 누적 GPL 직접 link 0건 재확인). 기존 챕터 본문 통합은 다음 리팩터(v0.3) 사이클로 이연. WBS 4.4.4 운영/사용자 매뉴얼 chunked 진행 중. |
| 2026-04-28 | R46 완료(audit only — security-auditor 에이전트): WBS 4.4.3 보안 점검. R28~R45 누적 22 findings — Critical 2(FIND-001 withApi 47개 미적용 / FIND-002 me/password 정책 우회) + High 5(FIND-003 middleware demo-public 첨부 누출 / FIND-004 DEV 평문 계정 / FIND-014 login rate limit 부재 / FIND-019 다운로드 감사 부재) + Medium 9 + Low 4 + Info 2. 등급 PASS-WITH-CAVEATS — 운영 시작 차단 사유. `docs/SECURITY_AUDIT.md` 706 LOC 산출. P0 4건은 R47에서 즉시 fix. |
| 2026-04-28 | R47 완료(BE only — 보안 P0 fix): R46 audit FIND-001/002/003/004 + FIND-014 일괄 Mitigated. 41 mutating route에 `withApi({ rateLimit: 'api' })` wrap(grep 검증 누락 0건, 의도적 skip 2건만), me/password를 `validatePasswordWithHistory` + `buildPasswordChangeUpdate` + currentPassword bcrypt + history shift + ActivityLog `PASSWORD_CHANGE_SELF` 통합, middleware `isDemoPublic`에서 attachments 제거 / `/viewer/` NODE_ENV dev only / 신규 `lib/attachment-auth.ts` helper로 5개 라우트(file/preview.dxf/preview.pdf/thumbnail/print) requireUser+canAccess+blockIfInfected 적용, `findDevUser`에 `NODE_ENV === 'production'` 1차 차단 + `.env.example` `DEV_AUTH_FALLBACK=false` 명시, authorize 첫 줄 IP 기반 5/min rate limit + `LoginRateLimitedError`. 44 files +560/-260, 1 new(`attachment-auth.ts`). typecheck/build pass. SECURITY_AUDIT.md 상태 4건 Mitigated 갱신. 운영 시작 차단 사유 해소. P1 후속(FIND-005 90일 만료 RSC 가드 / FIND-006 reset-password 정책 / FIND-018 LOGIN_SUCCESS/FAIL 감사 / FIND-019 다운로드 감사 / `meta/route.ts` dev path 보강)은 다음 라운드. |
| 2026-04-28 | R48 완료(BE+FE 병렬 — 보안 P1 cleanup): FIND-005/006/018/019 + meta dev path + FE rate_limited 매핑 일괄 Mitigated. backend가 (1) middleware에 `x-pathname` 헤더 forwarding 추가 + `(main)/layout.tsx`에서 path 추출 후 `/settings|/logout` skip + 만료 시 `/settings?tab=password&forced=1` redirect (2) admin reset-password에 `validatePassword(tempPassword)` + `passwordChangedAt: new Date(0)` 즉시 만료 (3) auth.ts authorize 전 분기(password/samlBridge/mfaBridge/rate_limited/mfa_required/account_locked)에 LOGIN_SUCCESS/LOGIN_FAIL + metadata 기록 (4) attachment 4개 라우트(file/preview.dxf/preview.pdf/print)에 OBJECT_DOWNLOAD/PREVIEW/PRINT 기록(thumbnail은 list 폭주 skip, preview.pdf는 no-Range read만) (5) meta/route를 requireUser 가드로 익명 차단. frontend가 login-form mapErrorCode에 rate_limited case 추가. 10 files +273/-10. typecheck/build pass. 잔여 P1: FIND-009(trustHost+useSecureCookies)만. P0/P1 거의 모두 해소 — 운영 시작 준비 완료. |
| 2026-04-28 | R49 완료(BE only — P1 잔여 + P2 묶음): FIND-009/021/011/012/008 일괄 Mitigated. (1) auth.config.ts에 `trustHost: true` + `useSecureCookies: prod-only`. (2) middleware의 보안 헤더 set 블록에 prod-only HSTS `max-age=31536000; includeSubDomains`. (3) `MAX_UPLOAD_BYTES`를 `parseInt(process.env.ATTACHMENT_MAX_BYTES ?? 200MB)`로 env-driven 변경 + `.env.example` 추가. (4) 신규 `lib/mime-allowed.ts` ALLOWED_MIME_TYPES + `isAllowedMimeType` → uploads route는 `z.enum(...)`, multipart objects/[id]/attachments는 `file.type` 가드. octet-stream은 fallback 허용 + ClamAV 콘텐츠 검사. (5) 신규 `lib/crypto.ts` AES-256-GCM envelope + scrypt(AUTH_SECRET, 'drawing-mgmt-totp-v1') + `v1:<iv>:<tag>:<cipher>` format → enroll이 totpSecret 저장 직전 `encryptSecret`, verifyTotp가 `isEncryptedSecret` 자동 분기. 평문 row backward-compat (다음 enroll 시 자연 마이그). schema.prisma 주석만 갱신(컬럼 변경 없음 — 마이그 0016 미발행). 11 files +322/-13. typecheck/build pass. **P0 + P1 전부 해소** — 보안 운영 시작 차단 사유 완전 0건. 잔여 P2 = FIND-007 SAML replay / FIND-013 Redis-backed RL / FIND-015 CSP nonce / FIND-022 backup 암호화. |
| 2026-04-28 | R50 완료(BE only — 잔여 P2 묶음): FIND-013/022/007 Mitigated. (1) `lib/rate-limit.ts` IORedis 싱글톤 + INCR/EXPIRE + ready 체크 + in-memory fallback. `rateLimit()` async 전환 → 호출자(auth.ts:344, api-helpers.ts:91) `await` 적용. REDIS_URL 미설정 또는 ready 안 됐을 때 in-memory fallback. (2) `apps/worker/src/backup.ts`가 `BACKUP_ENCRYPTION_KEY` 있을 때 pg_dump/tar 출력을 openssl `aes-256-cbc -salt -pbkdf2 -iter 100000` 파이프로 통과 + `.enc` 접미사. 미설정 시 평문(R33 호환). prune은 prefix 매칭이라 `.enc`와 양립. DEVELOPER_GUIDE.md §10.6에 복구 절차 코드 블록 추가. (3) 신규 `lib/saml-cache.ts` node-saml v5 `CacheProvider` async 인터페이스(saveAsync/getAsync/removeAsync) Redis 구현 — `saml:rid:<id>` TTL 10분 + SET NX EX. saml.ts `getSamlConfig`가 REDIS_URL 있으면 `validateInResponseTo: 'always'` + `cacheProvider`, 없으면 'never' fallback. 8 files +509/-46. typecheck/build pass. **잔여 P2 = FIND-015(CSP nonce)만**, RSC 영향으로 Phase 2 이연. |
| 2026-04-28 | R51 완료(docs + infra 병렬 — WBS 4.4.4 + 4.4.5 부분): (A, PM 직접) `docs/manuals/operations.md` 신규 370 LOC — 배포(Day 0 + 롤링 + 롤백) / 환경 변수 체크리스트 / 백업 자동·수동·복구·재해 시나리오 / 모니터링(일1회 admin 페이지 + 헬스체크 + 디스크) / 사용자 관리(잠금 해제·만료·MFA 비활성·폐기) / 트러블슈팅 11종 / SSO 운영(Keycloak + SAML) / 보안 운영(audit 점검·ActivityLog forensics·백업 키 관리) 9 챕터. (B, backend agent) `docker-compose.prod.yml`에 `nginx:1.27-alpine` 서비스 추가(80/443 + healthcheck + cert mount), `web`의 `ports: 3000:3000`을 `expose: 3000`으로 전환해 호스트 노출 차단, 신규 `ops/nginx/nginx.conf`(TLS 1.2/1.3 + OWASP intermediate cipher + HSTS 1y + HTTP→HTTPS 301 + WebSocket/SSE upgrade + gzip + 256m body cap + /health edge), 신규 `ops/nginx/.gitignore`(certs/* ignore + Let's Encrypt/사내 CA/자체서명 가이드 인라인) + `certs/.gitkeep`, `.env.example`에 `NEXT_PUBLIC_BASE_URL` 추가. compose config 파싱 검증 통과. typecheck 영향 없음(infra only). 머지는 3-way로 operations.md(main) + nginx(worktree) 양쪽 보존. |
| 2026-04-28 | R52 완료(docs + ETL 병렬 — WBS 4.3): (A, PM 직접) `docs/MIGRATION_PLAN.md` 235 LOC — 매핑 가정 v0(User/Folder/Org/Group/Object/Revision/Attachment) + ETL 실행 절차(검증환경/차이보정/cutover/검증) + 롤백 시나리오(부분/광범위/corruption) + 운영팀 합의 6항목(과거 결재/활동로그/폐기항목/임시비번/충돌/사용자동의) + 산출물 체크리스트. (B, backend agent) 신규 `packages/migration/` 패키지 — Source/Loader/Pipeline 추상화 + 50건 mock fixture(10 user/5 folder/50 drawing) + dry-run/full/verify/rehearsal CLI(process.argv 직접 파싱) + SHA-256 checksum + JSON report 출력. 28 files, 신규 deps 0건(node 표준 + @prisma/client만). 37/37 vitest pass. 실 TeamPlus dump 인계 시 `src/source/teamplus.ts` 어댑터만 갈아끼우면 라이브 모드 활성(단 `externalId` schema 델타 prerequisite — TODO 주석 명시). web/worker/shared/migration 4 패키지 typecheck pass. 머지 3-way로 MIGRATION_PLAN.md(main) + ETL 골격(worktree) 양쪽 보존. |
| 2026-04-28 | R53 완료(PM 직접 — WBS 4.4.2): k6 부하 테스트 script 3종 + README. `ops/loadtest/smoke.js` (1 VU × 1m, p95<500ms + http_req_failed<1%), `ops/loadtest/concurrent-users.js` (5 VU × 5m, 검색→상세→activity→thumbnail→unread-count, p95<800ms), `ops/loadtest/conversion-burst.js` (1 VU × 50 iter, 기존 ConversionJob retry burst, 95%+ enqueued + teardown에서 status counts 출력). README에 k6 설치/실행/threshold/CI 통합 가이드. k6는 외부 도구(AGPL) — 앱 link 없음, LibreDWG subprocess 격리와 동일 패턴. 4 files +400 LOC. typecheck 영향 없음. CI 자동 트리거는 비용으로 미적용 — manual 또는 별 워크플로. |
| 2026-04-28 | R54 완료(PM docs + FE 폴리시): (A, PM) 신규 `README.md` 최상위 — 1분 quick-start(개발자/운영자) + 핵심 문서 인덱스 + 모노레포 구조 + 보안 자세 + Phase 2 백로그. 기존 `docs/잔여작업.md` 헤더에 R7~R53 누적 안내 + §6 마일스톤 표 + §7 Phase 2 백로그(운영 시작 후 후속) 신설(F4 본문은 처리된 역사적 카드로 보존). (B, frontend agent) `apps/web/components/admin/VulnerabilitiesTable.tsx` polish — `SortableHeader` 컴포넌트 + severity/package/title 클릭 정렬(aria-sort 동기, 키보드 nav, ArrowUp/Down 아이콘) + [CSV 내보내기] 버튼(UTF-8 BOM + RFC 4180 escape + CRLF + filtered/sorted 데이터 + filename `vulnerabilities-YYYY-MM-DD.csv`). 1 file +290/-46. typecheck/build pass. R45에서 Phase 2로 이연한 USER_MANUAL v0.3 통합 리팩터는 잔여작업.md §7에 기록. |
| 2026-04-28 | R55 완료(FE only — 사용자 QA 보고 P0+P1+P2 일괄 fix): 사용자가 데스크톱/노트북/태블릿/모바일 4 viewport에서 직접 테스트 후 9건 이슈 보고 → 그 중 6건(P0 2 + P1 3 + P2 1) 이번 라운드에서 처리. (1) [QA-P0-1] 반응형 한글 깨짐: page.tsx 헤더/카드 라벨에 `break-keep` + `whitespace-nowrap` + grid를 `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`로 분기. search/page.tsx도 동일. break-keep 30회 적용 확인. (2) [QA-P0-2] React `Maximum update depth`: CommandPalette의 중복 ⌘K listener 제거(이게 P2-6 Cmd+K 미동작도 동시에 해결!), NotificationBell `onLoadMore`를 useCallback으로 안정화, NotificationPanel의 IntersectionObserver useEffect dep에서 onLoadMore 제외 + loadMoreRef 우회. (3) [QA-P1-3] 대시보드 STAT_CARDS 하드코딩 제거: `loadStatCounts(userId)` 신규 함수가 prisma로 4 카운트 쿼리(결재 대기/체크아웃/미해결/최근 배포 7일). DB 예외 시 0 fallback. (4) [QA-P1-4] 알림 팝오버: PopoverContent에 `z-50 bg-bg shadow-lg border` 보강, isPending 가드를 `fetchStatus !== 'idle'`로 축약 → enabled:false 시 무한 스켈레톤 차단. (5) [QA-P1-5] /objects/:id 404: 에러/no-obj 게이트를 로딩 게이트보다 먼저 체크 → 404 직후 추가 1-tick 스켈레톤 깜박임 제거. (6) [QA-P2-6] Cmd+K Mac: useKeyboardShortcuts가 이미 `metaKey || ctrlKey` 인식했지만 중복 listener가 net-zero 토글을 발생시켰음 → P0-2 fix가 자동 해결. Header.tsx kbd hint를 userAgent 기반으로 ⌘K(Mac)/Ctrl K(Win) 분기. 7 files +234/-72. typecheck/build pass + dev 재기동 후 console "Maximum update depth" 경고 0건 확인. 잔여 Q는 P1-4 환경설정 라우팅 회귀 테스트(별 라운드) + P2 빈 상태 일러스트 안내 텍스트 보강(별 라운드). |
| 2026-04-28 | R56 완료(PM 직접, FE polish — QA P2 잔여): (1) [QA-P1-4 잔여] `/settings?tab=notifications`로 알림 탭 즉시 활성화 — settings/page.tsx에 `useSearchParams` + VALID_TABS 화이트리스트 + 잘못된 값 'profile' fallback. NotificationPanel footer "환경설정" 링크의 href에 `?tab=notifications` 추가. (2) [QA-P2 빈 상태 보강] approval 4개 박스(대기/처리완료/상신함/회수)와 lobby 3개 박스(received/sent/expired)에 EmptyState `title` + `description` 박스별 분기 — 사용자가 어떤 행동을 해야 그 함이 채워지는지 한 줄 안내. EmptyState 컴포넌트는 이미 description prop 지원이라 텍스트만 추가. 4 files +60/-5. typecheck pass + 라우트 200 확인. |
