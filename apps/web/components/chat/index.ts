/**
 * R36 — public surface of the chat feature module.
 * AppShellClient + Header are the only callers; everything else lives
 * internal to the directory.
 */
export { ChatFab } from './ChatFab';
export { ChatPanel } from './ChatPanel';
export { RobotAvatar } from './RobotAvatar';
