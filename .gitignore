# Node / Next.js
node_modules/
.next/
out/
dist/
build/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# Env
.env
.env.local
.env.*.local

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Test / Coverage
coverage/
.nyc_output/

# Prisma
prisma/migrations/dev.db*

# Runtime data (local dev)
/data/
.data/
*.sqlite
*.sqlite-journal

# pnpm
.pnpm-store/

# Turbo / build artifacts
.turbo/
*.tsbuildinfo

# Sample fixtures (large)
fixtures/dwg/*.dwg
fixtures/pdf/*.pdf
fixtures/dxf/*.dxf
!fixtures/.gitkeep

# Harness workspace (intermediate artifacts: api_contract, design_spec, etc.)
_workspace/
_workspace_prev_*/
.claude/worktrees/
.claude/settings.local.json

# Playwright
apps/web/e2e/.auth/
apps/web/test-results/
apps/web/playwright-report/
.playwright-mcp/

# Local screenshots from manual verification
/login.png
/main.png

# R52 — migration package runtime artifacts.
# `migration-reports/` collects per-run JSON reports (one per dry-run/verify).
# `migration-storage/` is the local FILE_STORAGE_ROOT used by `full` mode.
# Both are wiped between rehearsals, so we don't want them in the repo.
migration-reports/
migration-storage/
packages/migration/migration-reports/
packages/migration/migration-storage/
